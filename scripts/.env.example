# OttoChain Node Secrets
# Copy to .env.local and fill in values (DO NOT commit .env.local!)
#
# On servers: /opt/ottochain/.env.local
# Locally: scripts/.env.local (for testing)

# REQUIRED: Server external IP
HOST_IP=

# REQUIRED: Keystore password (generate with: openssl rand -base64 24)
CL_PASSWORD=

# Optional: Key alias (default: alias)
# CL_KEYALIAS=alias

# Optional: Override directories
# JARS_DIR=/opt/ottochain/jars
# KEYS_DIR=/opt/ottochain/keys
# DATA_DIR=/opt/ottochain/data

# Optional: Docker network name
# NETWORK=ottochain_ottochain

# Optional: Java image
# JAVA_IMAGE=eclipse-temurin:21-jdk
